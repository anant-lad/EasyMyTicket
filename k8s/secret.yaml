# This file is a TEMPLATE — do NOT commit real values.
# Secrets are pulled from AWS Secrets Manager by the pods at runtime via IRSA.
# Only use this for local testing with base64-encoded placeholder values.
#
# For production: use ExternalSecrets operator (see external-secret.yaml)
# OR set these via:
#   kubectl create secret generic ticketing-secrets -n ticketing \
#     --from-literal=GROQ_API_KEY=gsk_... \
#     --from-literal=DB_PASSWORD=... \
#     --from-literal=SUPPORT_EMAIL_APP_PASSWORD=... \
#     --from-literal=REDIS_AUTH=... \
#     --from-literal=API_KEYS="key1,key2"

apiVersion: v1
kind: Secret
metadata:
  name: ticketing-secrets
  namespace: ticketing
type: Opaque
data:
  # base64-encode with: echo -n "value" | base64
  GROQ_API_KEY: ""
  DB_PASSWORD: ""
  SUPPORT_EMAIL: ""
  SUPPORT_EMAIL_APP_PASSWORD: ""
  REDIS_AUTH: ""
  API_KEYS: ""
